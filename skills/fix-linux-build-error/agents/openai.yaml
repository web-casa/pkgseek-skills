interface:
  display_name: "Fix Linux Build Errors"
  short_description: "Diagnose missing Linux packages, files, and commands"
  default_prompt: "Use $fix-linux-build-error to diagnose this build failure with distro-specific evidence."
dependencies:
  tools:
    - type: "mcp"
      value: "pkgseek"
      description: "PkgSeek Linux package and distribution intelligence"
      transport: "streamable_http"
      url: "https://api.pkgseek.com/mcp"
