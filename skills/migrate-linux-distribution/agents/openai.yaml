interface:
  display_name: "Migrate Linux Distribution"
  short_description: "Plan package-aware Linux distribution migrations"
  default_prompt: "Use $migrate-linux-distribution to plan this Linux distribution migration."
dependencies:
  tools:
    - type: "mcp"
      value: "pkgseek"
      description: "PkgSeek Linux package and distribution intelligence"
      transport: "streamable_http"
      url: "https://api.pkgseek.com/mcp"
