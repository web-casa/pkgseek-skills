interface:
  display_name: "Debug ELF and ABI"
  short_description: "Investigate Linux shared-library and ABI failures"
  default_prompt: "Use $debug-elf-abi to diagnose this ELF or shared-library failure."
dependencies:
  tools:
    - type: "mcp"
      value: "pkgseek"
      description: "PkgSeek Linux package and distribution intelligence"
      transport: "streamable_http"
      url: "https://api.pkgseek.com/mcp"
